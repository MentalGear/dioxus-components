target

**/.claude/settings.local.json
node_modules
package-lock.json

# Playwright
/test-results/
/playwright/playwright-report/
/playwright/test-results/
/blob-report/
/playwright/.cache/
# Session-local Playwright config: hardcodes one container's Chromium path and
# dev-server port, so it is per-session by design. dev-docs/dev-loop.md documents
# how to recreate it.
/playwright/session.local.config.ts
**/._*

# macOS
**/.DS_Store

# cargo-mutants report artifacts
mutants.out*/

# Subagent lane scratch (local to a session's container, never committed):
# git worktrees under .claude/worktrees/agent-<id> and their isolated
# CARGO_TARGET_DIR siblings. `target` above does not cover `target-lane-*`.
.claude/worktrees/
target-lane-*/
