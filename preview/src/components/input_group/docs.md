The Input Group component wraps an `InputGroupInput` with leading/trailing `InputGroupAddon`s (icons, buttons, static text) inside one bordered control.

## Component Structure

```rust
InputGroup {
    InputGroupAddon {
        Search {}
    }
    InputGroupInput { placeholder: "Search..." }
}
```
