The Kbd component displays a keyboard key or shortcut. `KbdGroup` lays out several `Kbd`s that together spell one shortcut.

## Component Structure

```rust
KbdGroup {
    Kbd { "Ctrl" }
    Kbd { "B" }
}
```
