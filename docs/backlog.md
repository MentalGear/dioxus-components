# General backlog — ranked, with triggers

Everything outstanding across the plan, the harness, and follow-ups discovered during execution — ranked by take-order (value weighted by effort), each with the **trigger** that makes it actionable. Component candidates live in [`component-backlog.md`](./component-backlog.md) (own queue, own triggers); this list covers everything else. Status ground truth: plan Phases 0–3 are merged (PR #8), the Phase 4 evidence is merged (PR #9), and the Phase 4 architecture (uniform API, narrow-leaf renderer split) is **approved**.

| # | Item | Trigger | Effort | Notes |
|---|---|---|---|---|
| 1 | Scroll-lock scrollbar-gutter fix (permanent `scrollbar-gutter: stable` baseline) | **Approved — in flight** | S | Fixes a real 15px shift shipping today; construction proven in spike round 2 |
| 2 | **Phase 4.4** — `popover`/`popover="auto"` top layer for non-modal overlays | **Landed** (Tooltip/HoverCard → `popover="manual"`; non-modal Popover → `<dialog popover="auto">`) | M | Fixes clipping in `overflow:hidden`/transformed ancestors (`playwright/oracle/tier2-html/top-layer.spec.ts` rule 1, red → green); also required a CSS-Anchor-Positioning addition (not originally scoped, but necessary: top-layer promotion changes containing block to the viewport) — Phase 5 collision/flip logic still not done, and CSS Anchor Positioning is Chromium-only as of this writing, so Firefox/WebKit fall back to un-anchored (still non-clipped) placement pending item 4 below. Popover-based rendering also cuts the CSS close-fade animation short (shown/hidden is driven by the immediate `open` signal, not the delayed animation-hold signal) — a minor, undone follow-up. Unblocks nested-submenu work in the component queue |
| 3 | **Phase 4.2** — native `<dialog>`+`showModal()` for Dialog and AlertDialog | **Landed** (modal `Dialog`/`AlertDialog` web arm → real `<dialog>` + `showModal()`; native/Blitz arm and non-modal `Dialog` unchanged) | M–L | Narrow-leaf `#[cfg(target_family = "wasm")]` split per Construction B (`docs/phase4-spike-findings.md`), reusing 4.4's axis and axis-swap caveat; close/cancel sync + `dialog.open` guard via new `use_dialog_close_sync`/`use_dialog_open_driver` (`lib.rs`); real child-component split (`DialogContentModal`/`DialogContentNonModal`, mirroring `popover.rs`) rather than a gated callback, confirming 4.4's warning that `use_global_keydown_listener`'s unconditional `preventDefault()` would otherwise fight the native `cancel` event; scroll lock kept as-is (proven sufficient in spike round 2). New backdrop-click dismiss (`use_dialog_backdrop_dismiss`, rect-based, since a `showModal()` backdrop click's `event.target` is the dialog itself, defeating `use_outside_dismiss`'s containment check). No `focus_scope.rs`/`aria_hidden.rs` existed in this repo to delete (recommended-implementations.md's more general framing referred to forks that never got ported here); the vendored `focus-trap.ts`/`.js` pair stays live — still used by the native/Blitz arm and by `Popover`'s (untouched) modal arm on both targets. New cosmetic gap, same treatment as row 19: `Sheet`'s slide-out animation (defined on the content element, now a `<dialog>`) is cut short by the browser's immediate `display:none` on close, ahead of `use_animated_open`'s unmount-hold |
| 4 | First 3-browser CI run — treat as calibration | Fires automatically on the next PR (Actions now enabled): firefox/webkit will execute every new oracle spec for the first time | S–M | Expect engine surprises (spike hazards were Chromium-only findings); budget one fix round |
| 5 | Tier-2 harness completion — rules 5 (`checkValidity`/`reportValidity`/`willValidate`), 7 (label association), 8 (`form` attribute) | Ready | S–M | Rule 8 pairs with adding the `form` attribute forwarding deliberately deferred in Phase 1 |
| 6 | Upstream engagement — where fixes land (open decision #1) + talking to upstream/fork authors (decision #4) | **User decision** | S to start | The 4.1 question is now evidence-backed and effectively a ready PR description; upstream `main` idle since 2026-06; consolidation with dignifiedquire/sarendipitee worth more than further cherry-picks |
| 7 | Wervice tooltip fade-out (`a704c517`, upstream PR #293) | **Unblocked** — plan gated it on 2.4 + generation counter, both landed | S | Cherry-pick with authorship, regression test per DoD |
| 8 | Select trigger → APG select-only-combobox role (`role="combobox"`) | Ready; schedule alongside other Select work | M | Restores AT-visible requiredness dropped with the invalid `aria-required`; touches existing spec selectors |
| 9 | Select listbox scroll lock | After 4.4 settles overlay architecture | S | Neither source fork covered it; noted in tier-3 oracle header |
| 10 | **Phase 5** — collision detection | **User decision 5.1 — REFRAMED by the 2026-09-01 bits-ui/Radix source comparison**: both delegate to Floating UI (identical flip/shift/size/arrow/hide pipelines; bits-ui is a line-for-line Svelte port of Radix's Popper). Since CSS Anchor Positioning is now in our stack, `position-try-fallbacks: flip-block, flip-inline` covers the FLIP sub-problem natively (Chrome/Edge 125+, FF 147+, Safari 26+), zero JS, additive to the existing @supports blocks — adopt on user go. Remaining 5.1 scope shrinks to: shift/size clamping, legacy-engine flip via the JS fallback, and ContextMenu's point-anchor clamp (5.2 — NOT solvable by CSS anchors, needs the virtual-anchor JS path) | S (flip) + M (rest) | Keep the CSS clamp (5.3) regardless. Two adoptable ideas from the comparison: bits-ui's debounced nested-lock cleanup (~24ms, survives same-tick unmount/remount races), and Radix's right-click carve-out for backdrop dismiss |
| 11 | Phase 6 — typeahead for menus (`dq typeahead.rs`, **not** `select/`) | Ready | S–M | Also a prerequisite-ish for Command palette in the component queue |
| 12 | Phase 6 — `pub mod portal`, `Toggle` `class` prop, public `CalendarDayState` (upstream #199) | Ready | S each | Small API surface items |
| 13 | Phase 6 — RTL (`dq direction.rs` + key-flip concept) | An RTL fixture decision (none exists) | M | |
| 14 | Nudge Dioxus framework issues: #2962 (`onclose`), #4319 (dialog methods), ancestor-traversal gap (`complaints.md`) | User decision (outward-facing) | S | Would replace our eval bridges with typed API; nothing blocks on it |
| 15 | `scrollbar-gutter` on older WebKit + iOS momentum scroll | A product decision that those targets matter | M | Both documented as known limits in `scroll_lock.rs`/findings; old engines simply keep today's behavior |
| 16 | **Component candidates** — new components from upstream PRs, the fork network, and catalog gaps | A decision to expand the component catalog | S–XL per item | Ranked queue with per-item triggers in [`component-backlog.md`](./component-backlog.md); three items are ready the moment expansion is wanted (ComboboxMulti, Field+Alert, Password Toggle Field) |
| 17 | Mutation testing (`cargo-mutants`), scoped to the algorithmic modules that already carry unit tests (slider math, calendar, virtualizer, `text_search`, selection, pointer, …) | **Baseline DONE 2026-08-31** — 37% mutation score, 397 survivors, ~350 genuinely unit-killable (see [`mutants-baseline.md`](./mutants-baseline.md)); next: the eight targeted test additions there, then commit the scoped command as a workflow_dispatch job (recommendation adopted: not nightly) | S to set up | Automates what the red-first discipline does by hand — a surviving mutant is exactly the "test cannot go red" finding Phase 2 hit twice (slider 2.1, virtual_list 2.2). Do NOT scope it over component render code: those mutants are only killable by the Playwright suite, and a wasm rebuild + e2e run per mutant is economically impossible; the oracle red-first rule covers that layer instead |
| 18 | CSS Anchor Positioning cross-browser fallback for the three Phase-4.4 overlays | Item 4 (3-browser CI run) will surface this concretely | S–M | `primitives/src/top_layer.rs`'s `anchor_name_style`/`position_anchor_style` + the `@supports (anchor-name: --a)`-gated rules in `preview/src/components/{tooltip,hover_card,popover}/style.css` only anchor content to its trigger on engines implementing CSS Anchor Positioning (Chromium-family as of 2026-08). Elsewhere, popover-promoted content still escapes clipping (rule 1 of the oracle) but falls back to the UA default (`position: fixed; inset: 0; margin: auto` — viewport-centered), not trigger-relative. Not a regression (pre-4.4 behavior didn't escape clipping at all there either), but a visual gap worth a product decision once non-Chromium engines are actually exercised |
| 19 | Popover/Tooltip/HoverCard close-fade animation cut short on the web (popover) arm | Discovered building 4.4; not fixed (would need `use_animated_open`-style coordination with `hidePopover()`, out of this slice's scope) | S | `use_popover_sync` calls `hidePopover()`/toggles `data-state` the instant `open` flips false, not after the CSS close animation `use_animated_open` otherwise holds the element in the DOM for — the browser applies `display: none` via its `[popover]:not(:popover-open)` UA rule immediately, so the fade-out CSS transition these three components' stylesheets define no longer has time to play on the web arm. Purely cosmetic (native/Blitz arm unaffected); no oracle rule currently checks animation duration |

| 20 | date_picker e2e spec — none exists today (calendar.spec.ts does not cover DatePicker) | Ready | S–M | Found during mutation-kill work; segment typing/arrows/backspace verified only ad hoc |

| 21 | Residual env-only anchor flake: top-layer ColorPicker CSS-path test fails ~40% in this sandbox (initial anchor() freezes at a wrong offset, correlated with slow page loads; end users unaffected — the JS fallback compensates) | A different environment (real CI machine) to check reproducibility, or a Chromium anchor-positioning fix upstream | S to verify | Investigated 2026-09-01: hook-side settle-loop bug fixed, scroll-at-open quirk worked around test-side; this residue looks like a browser-build engine bug, deliberately not papered over |

| 22 | SSG lane in CI — run the Playwright suite (at minimum `oracle/hydration-parity.spec.ts`) against the fullstack-SSG-built artifact as its own CI job, not just the `dx serve` dev-server lane the existing `playwright.yml` covers | CI reactivation (all workflows are `workflow_dispatch`-only as of 2026-08-30) | S | Standing prevention for the 2026-09-01 production incident (`docs/recommended-implementations.md` Caveat 1): the cfg-axis bug that broke the deployed site shipped and passed every existing check, because nothing in CI ever built+served the SSG artifact and hydrated it in a real browser — the dev-server lane and the SSG lane can silently diverge in build shape (markup cfg axis, feature wiring) with no CI signal until deploy. `scripts/check-cfg-axis.sh` (wired into `main.yml`) catches a *recurrence* of the specific cfg-axis mistake at the source level; this item is the black-box backstop that would catch the *next*, differently-shaped SSG/hydration defect the same way. Recipe: `docs/conformance-harness.md`, "Hydration/deployment parity" |

Standing rule for every item: the definition-of-done loop from [`plan.md`](./plan.md) — rule-cited oracle red first, provenance per `lifting-from-forks.md` §1, checks green, ledger updated.
